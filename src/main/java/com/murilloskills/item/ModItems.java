package com.murilloskills.item;

import com.murilloskills.MurilloSkills;

/**
 * Custom items for the MurilloSkills mod.
 * Currently a placeholder for future item additions.
 */
public class ModItems {

    /**
     * Called during mod initialization to register custom items.
     */
    public static void registerModItems() {
        // No custom items registered yet
        MurilloSkills.LOGGER.debug("ModItems initialized (no custom items)");
    }
}
