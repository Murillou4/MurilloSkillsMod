# Fabric 1.16.5 Port

Status: runtime-port.

Built by `tools/build-all.ps1` into `dist/1.16.5/fabric`. Uses `multi-loader-runtime` with Fabric entrypoints, JSON player saves, core commands, and reflective Fabric event hooks.
