# Forge 1.12.2 Port

Status: runtime-port.

Built by `tools/build-all.ps1` into `dist/1.12.2/forge`. Uses `multi-loader-runtime` with Forge 1.12 metadata, legacy Forge entrypoint, JSON player saves, core commands, and reflective event hooks.
