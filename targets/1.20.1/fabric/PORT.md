# Fabric 1.20.1 Port

Status: runtime-port.

Built by `tools/build-all.ps1` into `dist/1.20.1/fabric`. Uses `multi-loader-runtime` with Fabric entrypoints, JSON player saves, core commands, and reflective Fabric event hooks.
