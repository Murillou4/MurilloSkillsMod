# Forge 1.19.2 Port

Status: runtime-port.

Built by `tools/build-all.ps1` into `dist/1.19.2/forge`. Uses `multi-loader-runtime` with Forge metadata, Forge entrypoint, JSON player saves, core commands, and reflective event hooks.
